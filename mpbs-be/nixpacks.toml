# nixpacks.toml

[phases.setup]
providers = ['...', 'swag']
nixPkgs = ['...']

[phases.build]
cmds = [
    'echo Hello!',
    '../mpbs-be/generate-be.sh',
    'echo Hello 3!',
    'swag init main.go --output docs',
    'go build -o out'
]

[start]
cmd = './out'