# nixpacks.toml

[phases.setup]
providers = ['...', 'swag']
nixPkgs = ['...']

[phases.build]
cmds = [
    '../mpbs-protobuf/generate-be.sh',
    'swag init main.go --output docs',
    'go build -o out'
]

[start]
cmd = './out'